# Backend API base URL
# Point this at the backend origin, not /api.
VITE_API_URL="http://localhost:3000"

# Optional: kept for the existing deploy script / Docker build inputs.
# The current frontend code gets the LiveKit wsUrl from backend API responses.
VITE_LIVEKIT_WS_URL="wss://your-project.livekit.cloud"
